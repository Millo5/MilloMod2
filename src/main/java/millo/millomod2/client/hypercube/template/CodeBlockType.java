package millo.millomod2.client.hypercube.template;

public enum CodeBlockType {

    PLAYER_EVENT,
    PLAYER_ACTION,
    IF_PLAYER,

    ENTITY_EVENT,
    ENTITY_ACTION,
    IF_ENTITY,

    GAME_EVENT,
    GAME_ACTION,
    IF_GAME,

    FUNCTION,
    PROCESS,
    CALL_FUNCTION,
    START_PROCESS,

    SET_VARIABLE,
    IF_VARIABLE,
    ELSE,

    SELECT_OBJECT,
    REPEAT,
    CONTROL

}
